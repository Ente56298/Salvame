import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.asistentevial.mexico',
  appName: 'Asistente Vial Mexico',
  webDir: 'build'
};

export default config;
